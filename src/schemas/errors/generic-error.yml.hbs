# Generated from template - do not manually edit
title: {{ title }}
description: Error returned from the API server
type: object
properties:
  statusCode:
    type: number
    description: The HTTP status code
    example: {{ code }}
  error:
    type: string
    description: The HTTP status message
    example: {{ error }}
{{#if message }}
  message:
    type: string
    description: |
      A message from the server indicating the cause or possible resolution of the error.
    example: {{ message }}
{{/if}}
required:
  - statusCode
  - error
